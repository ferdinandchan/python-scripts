# python-scripts
A repo for different python scripts

# Local setup
- make sure you have python3 installed. For mac, suggest you install via [Homebrew](https://brew.sh/)

    ```brew install python```
